import styled from "styled-components";

export default function Logo() {
  return <Wrapper>MyWallet</Wrapper>;
}

const Wrapper = styled.div`
  font-family: "Saira Stencil One", cursive;
  font-weight: 400;
  font-size: 32px;
  line-height: 50px;
  color: #ffffff;
  margin-bottom: 25px;
`;
